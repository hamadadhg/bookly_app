/*
import 'package:bookly_app/core/utils/styles/style_to_colors.dart';
import 'package:flutter/material.dart';
import 'package:shimmer/shimmer.dart';

class CustomHorizontalImageShimmer extends StatelessWidget {
  const CustomHorizontalImageShimmer({super.key});

  @override
  Widget build(BuildContext context) {
    return Shimmer.fromColors(
      baseColor: StyleToColors.grey700Color,
      highlightColor: StyleToColors.grey400Color,
      child: AspectRatio(
        aspectRatio: 2 / 3,
        child: Container(
          decoration: BoxDecoration(
            borderRadius: BorderRadius.circular(
              16,
            ),
            color: StyleToColors.whiteColor,
          ),
        ),
      ),
    );
  }
}
*/
