/*
import 'package:bookly_app/core/utils/styles/style_to_text.dart';
import 'package:flutter/widgets.dart';

class CustomTextInitialState extends StatelessWidget {
  const CustomTextInitialState({
    super.key,
    required this.textInitialState,
  });
  final String textInitialState;
  @override
  Widget build(BuildContext context) {
    var height = MediaQuery.of(context).size.height;
    return Center(
      child: Text(
        textInitialState,
        style: StyleToText.textStyle16.copyWith(
          fontSize: height * 0.025,
        ),
        textAlign: TextAlign.center,
      ),
    );
  }
}
*/
