/*
import 'package:bookly_app/core/utils/styles/style_to_text.dart';
import 'package:flutter/widgets.dart';

class CustomTextFailureState extends StatelessWidget {
  const CustomTextFailureState({
    super.key,
    required this.textFailureState,
  });
  final String textFailureState;
  @override
  Widget build(BuildContext context) {
    var height = MediaQuery.of(context).size.height;
    return Center(
      child: Text(
        textFailureState,
        style: StyleToText.textStyle16.copyWith(
          fontSize: height * 0.025,
        ),
        textAlign: TextAlign.center,
      ),
    );
  }
}
*/
